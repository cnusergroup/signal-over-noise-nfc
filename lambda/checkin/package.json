{
  "name": "signal-hunt-checkin",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1054.0",
    "@aws-sdk/client-dynamodb": "^3.712.0",
    "@aws-sdk/client-scheduler": "^3.712.0",
    "@aws-sdk/lib-dynamodb": "^3.712.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "fast-check": "^3.15.0",
    "vitest": "^1.6.0"
  }
}
