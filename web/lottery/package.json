{
  "name": "signal-over-noise-lottery-web",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Frontend lottery-page ES modules for the Signal Over Noise After Party Lottery, plus their Vitest + fast-check tests.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "fast-check": "^3.15.0",
    "jsdom": "^24.1.3",
    "vitest": "^1.6.0"
  }
}
